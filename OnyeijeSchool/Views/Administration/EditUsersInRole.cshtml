@using OnyeijeSchool.ViewModel
@model  List<UserRoleViewModel>
@{
    var roleId = ViewBag.roleId;
    Layout = "~/Views/Shared/_Layout.cshtml";
}


                                <form method="post">
                                    <div class="card">
                                        <div class="card-header">
                                            <h2>Add or remove users from this role</h2>
                                        </div>
                                        <div class="card-body">
                                            @for (int i = 0; i < Model.Count; i++)
                                            {


                                                @*<div class="checkbox">
                            <label class="form-check-label" asp-for="@Model[i].IsSelected">
                                <input asp-for="@Model[i].IsSelected" />
                                @Model[i].Username
                            </label>

                        </div>*@
                                                <div class="togglebutton">
                                                    <label class="form-check-label" asp-for="@Model[i].IsSelected">
                                                        <input type="hidden" asp-for="@Model[i].UserId">
                                                        <input type="hidden" asp-for="@Model[i].Username">
                                                        <input type="checkbox" asp-for="@Model[i].IsSelected">
                                                        <span class="toggle"></span>
                                                        @Model[i].Username
                                                    </label>
                                                </div>


                                            }
                                        </div>
                                        <div class="card-body col-4">
                                            <input type="submit" value="Update" class="btn btn-primary btn-sm" style="width:auto" />
                                            <a asp-action="EditRole" asp-route-id="@roleId" class="btn btn-rose btn-sm" style="width:auto">Cancel</a>
                                        </div>
                                    </div>
                                </form>
                          